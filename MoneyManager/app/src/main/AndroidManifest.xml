<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alandevasia.moneymanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activityMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activityIncome" />
        <activity android:name=".incomelistviewop" />
        <activity android:name=".activityExpence" />
        <activity android:name=".ExpenseList" />
        <activity android:name=".IncExpView" />
        <activity android:name=".IncoView" />
        <activity android:name=".ExpeView" />

        <activity android:name=".Calendar"></activity>
    </application>

</manifest>